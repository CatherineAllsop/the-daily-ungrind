let subscribeForm = document.querySelector("#subscribe-form");
function subscribeForm (event) {
   event.preventDefault();
   let subscribeFormInput=document.querySelector ("#email-input");
}
subscribeForm.addEventListener("submit", subscribeForm)